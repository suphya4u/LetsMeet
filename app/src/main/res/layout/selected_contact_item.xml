<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    style="@style/selected_contact_item_content"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <ImageView
        android:id="@+id/selected_contact_thumbnail"
        android:layout_width="18dp"
        android:layout_height="18dp"
        android:contentDescription="@string/contact_image_help"
        style="@style/contact_item_contact_image"
        android:layout_gravity="center_vertical" />

    <LinearLayout
        android:layout_weight="1"
        android:layout_width="0dp"
        android:layout_height="fill_parent"
        android:orientation="vertical"
        android:gravity="center_vertical" >

        <TextView
            android:id="@+id/selected_contact_name"
            android:ellipsize="end"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

    </LinearLayout>

    <ImageButton
        android:id="@+id/remove_selected_contact"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_cancel_black_24dp"
        android:contentDescription="@string/cancel_contact_selection_help"
        android:layout_gravity="center_vertical"
        android:background="@null"
        android:tint="@color/material_icon_light"/>

</LinearLayout>
